name: 'Pharo CI'
description: 'To load and run tests from a pharo project'
inputs:
  removes-repo:  # id of input
    description: 'Name of packages to remove'
    required: true
    default: '.'
  baseline:  # id of input
    description: 'The baseline of the project to test'
    required: true
    default: '.'
  group:  # id of input
    description: 'the baseline group to load'
    required: true
    default: '.'

outputs:
  time: # id of output
    description: 'The current time'
runs:
  using: 'node16'
  main: 'index.js'